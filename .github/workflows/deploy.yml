name: Deploy to Cloudflare Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test
        run: node tests/calc.test.mjs
      - name: Remove non-public files
        # Pages の deploy は .assetsignore を見ないので、配信しないファイルはここで消す
        run: rm -rf tests scripts _sources _enseihi_draft.html.txt SOURCES.md .assetsignore wrangler.jsonc
      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          wranglerVersion: "4.85.0"
          command: pages deploy . --project-name=ninja-ensei --branch=main --commit-dirty=true
